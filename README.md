# WeYS

## 介绍

1. 一个基于 AutoHotkey 的脚本集合。
2. 一个基于bun+vue的前后端项目

## 快速开始

1. 下载并安装 **AutoHotkey** 软件。
2. 下载并安装**python 3.x**。运行 `启动OCR.bat`文件。
3. 运行 `yuanshen.ahk` 脚本。
4. [可选] 如果需要使用服务端，请先安装**bun**，然后进入 `server`目录，运行 `bun i`安装依赖，再运行 `bun dev`启动服务端。
